$(document).ready(function () {

/*
  $('.multifile').MultiFile({
    max: 5,
    accept: 'gif|jpg|png|bmp',
    STRING: {
      remove: 'удалить',
      denied: 'Вы не можете выбрать файл с расширением $ext',
      selected: 'Выбранный файл: $file',
      duplicate: 'Вы уже выбрали этот файл:\n$file'
    }
  });
*/
});
